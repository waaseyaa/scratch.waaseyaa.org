name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy-rsync:
    name: Deploy rsync rules
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify deploy rsync rules
        run: ./bin/verify-deploy-rsync
